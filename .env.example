# Copy to `.env` and fill in. `.env` is gitignored — never commit real keys.
# Loaded by the code starting in Phase 1 (teacher client); unused in Phase 0.

# Teacher: Claude API (Phase 1+).
ANTHROPIC_API_KEY=

# Experiment tracking for fine-tuning runs (Phase 3).
WANDB_API_KEY=

# Hugging Face access for gated student weights (Phase 3/5).
HF_TOKEN=
