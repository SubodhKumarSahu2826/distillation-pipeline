"""distill — fine-tuning model distillation pipeline.

Distills structured extraction (document → fixed JSON) from a teacher LLM into a
self-hosted student. See CLAUDE.md for the session protocol and docs/ for the plan.
"""

__version__ = "0.0.0"
